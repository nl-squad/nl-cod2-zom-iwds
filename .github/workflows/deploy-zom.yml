name: Deploy zom server files

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deployment job
    uses: nl-squad/nl-cod2-workflows/.github/workflows/deploy.yml@main
    with:
      profile: zom
    secrets: inherit
